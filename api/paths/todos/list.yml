get:
  security:
    - Bearer: []
  tags:
    - todo
  summary: ToDo一覧
  operationId: getTodos
  responses:
    "200":
      description: OK
      content:
          application/json:
            schema:
              type: array
              items:
                $ref: ../../schemas/todos/Todo.yml
